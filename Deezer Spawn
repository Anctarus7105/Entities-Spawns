function GitAud(soundgit,filename)

    SoundName=tostring(SoundName)

    local url=soundgit

    local FileName = filename

    writefile(FileName..".mp3", game:HttpGet(url))

    return (getcustomasset or getsynasset)(FileName..".mp3")

end

function CustomGitSound(soundlink, vol, filename)

    local sound = Instance.new("Sound")

    sound.SoundId = GitAud(soundlink, filename)

    sound.Parent = workspace

    sound.Name = "deezerspawn"

    sound.Volume = 10

    sound.PlaybackSpeed = 1

   sound:Play()

end

CustomGitSound("https://github.com/PABMAXICHAC/doors-monsters-models/raw/refs/heads/main/DeezerSpawned.mp3?raw=true", 1, "deezerspawn")

-- Check current room

local entityModel = true

local latestRoom = game.ReplicatedStorage.GameData.LatestRoom.Value

-- If in Seek, Figure, or Room 90+

if game.Workspace:FindFirstChild("SeekMovingNewClone") or 

   latestRoom == 50 or latestRoom == 100 or  

   (latestRoom >= 90 and latestRoom <= 100) then

    entityModel = false

end

local Lag = true

local depthsTer = game:GetObjects("rbxassetid://101498124045552")[1] -- Load DepthsTer from the ID

depthsTer.Parent = game.Workspace

depthsTer.Name = "Deezer"

local part = depthsTer:FindFirstChildWhichIsA("BasePart") or depthsTer:FindFirstChildWhichIsA("Part")

-- Set the position of DepthsTer in the room

part.CFrame = game.Workspace.CurrentRooms:GetChildren()[#game.Workspace.CurrentRooms:GetChildren() - 1].Parts.Floor.CFrame + Vector3.new(0, 6, 0)

wait(3)

task.spawn(function()

    while Lag == true do

        task.wait(0.2)  -- Reduce the wait time to increase damage speed

        local _, camera = game.Workspace.CurrentCamera:WorldToViewportPoint(part.Position)

        

        -- If the player is not looking at DepthTer, they take damage

        if not camera then

game.Players.LocalPlayer.Character.Humanoid:TakeDamage(0.3)

function GitAud(soundgit,filename)

    SoundName=tostring(SoundName)

    local url=soundgit

    local FileName = filename

    writefile(FileName..".mp3", game:HttpGet(url))

    return (getcustomasset or getsynasset)(FileName..".mp3")

end

function CustomGitSound(soundlink, vol, filename)

    local sound = Instance.new("Sound")

    sound.SoundId = GitAud(soundlink, filename)

    sound.Parent = workspace

    sound.Name = "dderzeing"

    sound.Volume = 1.5

    sound.PlaybackSpeed = 1.2

   sound:Play()

end

CustomGitSound("https://raw.githubusercontent.com/Voorproo/Silence/refs/heads/main/RPReplay_Final1748135502%20(mp3cut.net).mp3", 1, "deezer")

        end

        

        -- If the player dies, update the death cause and show the death hint

        if game.Players.LocalPlayer.Character.Humanoid.Health <= 0 then

            -- Show death hint

            firesignal(game.ReplicatedStorage.RemotesFolder.DeathHint.OnClientEvent, {"You died to who you call Deezer...","He spawns in center of room and he will damage you if you dont look at him","He is a starer"},"Blue")

            wait(0.01)

            -- Update the death cause in the game stats

            game:GetService("ReplicatedStorage").GameStats["Player_".. game.Players.LocalPlayer.Name].Total.DeathCause.Value = "Deezer"

        end

    end

end)

-- Wait for the room to change

game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()

Lag = false

function GitAud(soundgit,filename)

    SoundName=tostring(SoundName)

    local url=soundgit

    local FileName = filename

    writefile(FileName..".mp3", game:HttpGet(url))

    return (getcustomasset or getsynasset)(FileName..".mp3")

end

function CustomGitSound(soundlink, vol, filename)

    local sound = Instance.new("Sound")

    sound.SoundId = GitAud(soundlink, filename)

    sound.Parent = workspace

    sound.Name = "deezerdespawn"

    sound.Volume = 10

    sound.PlaybackSpeed = 1

   sound:Play()

end

CustomGitSound("https://github.com/PABMAXICHAC/doors-monsters-models/raw/refs/heads/main/DeezerJumpscare.mp3?raw=true", 1, "deezerdespawn")

-- Destroy DepthTer when no longer needed

game.Workspace.Deezer:Destroy()

-- Adding Fog If Chance

if game.Players.LocalPlayer.Character.Humanoid.Health = -25 or game.Players.LocalPlayer.Character.Humanoid.Health = -30 or game.Players.LocalPlayer.Character.Humanoid.Health = -35 or game.Players.LocalPlayer.Character.Humanoid.Health = -40 or game.Players.LocalPlayer.Character.Humanoid.Health = -45 = then
wait(0.4)
loadstring(game:HttpGet("https://pastebin.com/raw/XNvdaTMC"))()
